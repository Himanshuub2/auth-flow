"""Application-wide string constants for item types and related values."""

# Item types (combined events + documents API)
EVENT = "event"
DOCUMENT = "document"

# Valid item types for validation
ITEM_TYPES = (EVENT, DOCUMENT)
